# The local vertical slice defaults to the deterministic fake provider.
MODEL_PROVIDER=fake

# Configure a real provider only after D3 approval.
# MODEL_API_KEY=
# MODEL_NAME=

# Disable live generation without changing the UI contract.
LIVE_GENERATION_ENABLED=false
